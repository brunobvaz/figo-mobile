import { Text, View } from 'react-native';
import colors from '../../theme/colors';

export default function ExploreMap() {
  return <View style={{ flex: 1, padding: 16 }}><Text style={{ color: colors.textMuted }}>O mapa está disponível na aplicação móvel. Podes consultar estes produtos na Lista.</Text></View>;
}
