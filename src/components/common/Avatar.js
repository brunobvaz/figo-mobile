import { Image, StyleSheet, Text, View } from 'react-native'; import colors from '../../theme/colors';
export default function Avatar({ uri, name = '', size = 52 }) { const box = { width: size, height: size, borderRadius: size / 2 }; return uri ? <Image source={{ uri }} style={[styles.image, box]} /> : <View style={[styles.fallback, box]}><Text style={[styles.initials, { fontSize: size * 0.34 }]}>{name.split(' ').slice(0, 2).map((part) => part[0]).join('').toUpperCase()}</Text></View>; }
const styles = StyleSheet.create({ image: { backgroundColor: colors.primaryLightFigo }, fallback: { backgroundColor: colors.primaryLight, alignItems: 'center', justifyContent: 'center' }, initials: { color: colors.primaryDark, fontWeight: '700' } });
