import { FlatList, StyleSheet } from 'react-native'; import EmptyState from '../common/EmptyState'; import ProductCard from './ProductCard'; import spacing from '../../theme/spacing';
export default function ProductList({ products, onProductPress, horizontal = false, variant, contentContainerStyle }) { return <FlatList data={products} horizontal={horizontal} keyExtractor={(item) => item.id} showsHorizontalScrollIndicator={false} showsVerticalScrollIndicator={false} contentContainerStyle={[styles.content, variant && styles.homeContent, contentContainerStyle]} ItemSeparatorComponent={() => <></>} ListEmptyComponent={<EmptyState title="Sem produtos" message="Experimenta alterar a pesquisa ou os filtros." />} renderItem={({ item }) => <ProductCard product={item} variant={variant} compact={!horizontal} onPress={() => onProductPress(item)} />} />; }
const styles = StyleSheet.create({ homeContent: { paddingVertical: spacing.xs }, content: { gap: spacing.md, paddingVertical: spacing.sm } });
