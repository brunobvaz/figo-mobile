import { FlatList, StyleSheet } from 'react-native'; import EmptyState from '../common/EmptyState'; import ProductCard from './ProductCard'; import spacing from '../../theme/spacing';
export default function ProductList({ products, onProductPress, horizontal = false, contentContainerStyle }) { return <FlatList data={products} horizontal={horizontal} keyExtractor={(item) => item.id} showsHorizontalScrollIndicator={false} showsVerticalScrollIndicator={false} contentContainerStyle={[styles.content, contentContainerStyle]} ItemSeparatorComponent={() => <></>} ListEmptyComponent={<EmptyState title="Sem produtos" message="Experimenta alterar a pesquisa ou os filtros." />} renderItem={({ item }) => <ProductCard product={item} compact={!horizontal} onPress={() => onProductPress(item)} />} />; }
const styles = StyleSheet.create({ content: { gap: spacing.md, paddingVertical: spacing.sm } });
