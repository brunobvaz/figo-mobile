import { ScrollView, StyleSheet, View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import colors from '../../theme/colors'; import spacing from '../../theme/spacing';
export default function Screen({ children, scroll = false, contentContainerStyle, style }) { const Container = scroll ? ScrollView : View; return <SafeAreaView edges={['top']} style={[styles.safe, style]}><Container showsVerticalScrollIndicator={false} contentContainerStyle={scroll ? [styles.content, contentContainerStyle] : undefined} style={!scroll ? [styles.content, contentContainerStyle] : undefined}>{children}</Container></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: colors.background }, content: { flexGrow: 1, paddingHorizontal: spacing.md, paddingBottom: spacing.lg } });
